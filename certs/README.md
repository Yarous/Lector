# TLS Certificates

For development, self-signed certificates are generated automatically by the
`lector-transport` crate using `rcgen`. The scripts here are for production
deployments where you want pre-generated certificates.

```bash
bash generate.sh
```
